{{template "base" .}}

{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>This is the About Page</h1>
                <p>From the Data Template: {{index .StringMap "test"}}</p>
            </div>
        </div>
    </div>
{{end}}